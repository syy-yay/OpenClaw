apiVersion: apps/v1
kind: Deployment
metadata:
  name: openclaw-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openclaw-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: openclaw-app
    spec:
      volumes:
      - name: app-data
        persistentVolumeClaim:
          claimName: openclaw-pvc
      containers:
      - name: openclaw-app
        image: 192.168.1.200:5000/openclaw/myapp:e06c97c
        ports:
        volumeMounts:
        - name: app-data
          mountPath: /app/data
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: openclaw-app-svc
  namespace: default
spec:
  selector:
    app: openclaw-app
  ports:
        volumeMounts:
        - name: app-data
          mountPath: /app/data
  - port: 80
    targetPort: 5000
  type: NodePort
